"""Bundled validated physics core (immutable).

Present only so the core's ``.py`` file is shipped with installed copies of
the package; it is loaded by path (importlib), not imported as a submodule.
"""